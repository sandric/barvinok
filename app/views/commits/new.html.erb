<h1><%= @keyboard.name %></h1>

<%= react_component('Commit', 'commit',
	user: @user,
	keyboard: @keyboard,
	name: @parent_commit.name,
	description: @parent_commit.description,
	data: raw(@parent_commit.as_json),
	parent_id: @parent_commit.id,
	authenticity_token: form_authenticity_token) %>