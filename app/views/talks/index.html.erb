<h1>Talks for <%= @keyboard.name %> </h1>

<%= link_to 'Create new talk', new_keyboard_talk_path %>

<ul>
	<% @talks.each do |talk| %>
		<li> 

			<%= link_to talk.title, talk_path(talk) %> 

			<%= link_to 'Edit', edit_talk_path(talk) %> 

			<%= link_to 'Delete', talk_path(talk), method: :delete, data: { confirm: 'Are you sure?' } %>
		</li>
	<% end %>
</ul>