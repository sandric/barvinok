<% if activity.trackable && activity.owner %>
	<div>
  		Liked <%= link_to activity.trackable.name, user_keyboard_path(activity.owner.name, activity.trackable.name) %>
  	</div>
<% else %>
  	<div>
  		Liked keyboard that does not exist anymore.
  	</div>
<% end %>