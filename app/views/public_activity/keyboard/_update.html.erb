<% if activity.trackable && activity.owner %>
	<div>
  		Updated <%= link_to activity.trackable.name, user_keyboard_path(activity.owner.name, activity.trackable.name) %>
  	</div>
<% else %>
  	<div>
  		Updated a keyboard that does not exist anymore.
  	</div>
<% end %>